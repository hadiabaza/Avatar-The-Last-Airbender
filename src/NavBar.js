import React from 'react';

const NavBar = () => {
    return (
        <div>
            <button>Characters</button>
            <button>Favorites</button>
        </div>
    );
}

export default NavBar;
